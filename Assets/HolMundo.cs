using UnityEngine;

public class HolMundo : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
         Debug.Log("Hola mundo");   
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
